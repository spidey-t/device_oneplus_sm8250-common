# Allow display server to access ION allocator memory
allow surfaceflinger ion_device:chr_file r_file_perms;
# Allow SurfaceFlinger to handle system display state transitions
allow surfaceflinger hal_graphics_composer_default:fd use;
